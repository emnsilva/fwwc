# ASP.NET Core (.NET Framework)
# Build and test ASP.NET Core projects targeting the full .NET Framework.
# Add steps that publish symbols, save build artifacts, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/dotnet-core

trigger:
- master

pool:
  vmImage: 'windows-latest'

variables:
  solution: '**/*.sln'
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'

steps:

# Command line
# Run a command line script using Bash on Linux and macOS and cmd.exe on Windows

- task: CmdLine@2
  displayName: Adicionar domínio personalizado
  inputs:
    #script: 'mkdir d:\a\1\MobEAD' 
    script: 'Domínio adicionado'
    #workingDirectory: # Optional
    #failOnStderr: false # Optional

- task: CmdLine@2
  displayName: Importar .csv
  inputs:
    script: 'echo Arquivo .csv importado'

- task: CmdLine@2
  displayName: Organizar grupos e usuários
  inputs:
    script: 'echo Grupos e usuários organizados'

- task: CmdLine@2
  displayName: Configurar autenticação multifator
  inputs:
    script: 'echo MFA configurado'

- task: CmdLine@2
  displayName: Configurar autenticação multifator
  inputs:
    script: 'echo MFA configurado'