# ASP.NET Core (.NET Framework)
# Build and test ASP.NET Core projects targeting the full .NET Framework.
# Add steps that publish symbols, save build artifacts, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/dotnet-core

trigger:
- master

pool:
  vmImage: 'windows-latest'

variables:
  solution: '**/*.sln'
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'

steps:

# Command line
# Run a command line script using Bash on Linux and macOS and cmd.exe on Windows

- task: CmdLine@2
  displayName: Configurar o Observador de Rede
  inputs:
    #script: 'mkdir d:\a\1\MobEAD' 
    script: 'echo Watcher configurado'
    #workingDirectory: # Optional
    #failOnStderr: false # Optional

- task: CmdLine@2
  displayName: Configurar o Monitor do Azure
  inputs:
    script: 'echo Monitor configurado'


- task: CmdLine@2
  displayName: Configurar a Integridade de Serviço
  inputs:
    script: 'echo Concluído'

# - task: CmdLine@2
#   displayName: Configurar o Azure SIEM
#   inputs:
#     script: 'echo SIEM configurado'

# - task: CmdLine@2
#   displayName: Configurar as políticas do Azure
#   inputs:
#     script: 'echo Políticas configuradas'